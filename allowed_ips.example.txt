# One IP or CIDR per line. Leave this file empty to allow everyone
# (fine for local testing, not recommended in production).
# Bare IPs (e.g. 203.0.113.5) or full CIDR ranges (e.g. 203.0.113.0/24)
# are both accepted.
